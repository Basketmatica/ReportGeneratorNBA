# ── Web framework ──
fastapi>=0.115.0,<0.120.0
uvicorn[standard]>=0.30.0,<0.40.0

# ── Cliente HTTP (balldontlie + utilidades) ──
httpx>=0.27.0,<0.30.0

# ── nba_api: SOLO usamos su BD estática local de jugadores ──
# (no se usa para llamar a stats.nba.com, que está bloqueado en Render)
nba_api>=1.10.0

# ── Generación de PDF ──
weasyprint>=62.0,<70.0

# ── Google Gemini (SDK unificado) ──
google-genai>=1.0.0,<2.0.0
