# ── UI ──
streamlit>=1.36.0,<2.0.0

# ── Datos (balldontlie + ESPN, nba_api) + cliente LLM (OpenAI-compatible vía httpx) ──
httpx>=0.27.0,<0.30.0
nba_api>=1.10.0,<2.0.0

# ── Generación de PDF ──
weasyprint>=62.0,<70.0
